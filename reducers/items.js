// import * as types from '../actions/actionTypes';

const initialState = {
    value: []
};

export default function items(state = initialState, action) {
    switch (action.type) {
        case 1:
            return state;
    
        default:
            return state;
    }
}